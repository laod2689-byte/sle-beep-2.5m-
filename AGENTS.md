# AGENTS.md

1. 回答使用中文。
2. 进入这个仓库后，先阅读项目根目录的 `PROJECT_MEMORY.md` 和 `docs/DEBUG_PLAYBOOK.md`，再判断当前状态。
3. 如果可用，使用 `c-skill` 仓库里的 `sle-ranging-bs21e` skill。
4. 实际编译和烧录以用户当前桌面工程为准，不要把上传仓库或镜像目录误当成完整 SDK。
5. 不要默默覆盖用户改动；如果源码默认值和生成配置不一致，要明确指出。
6. 改蜂鸣器、阈值、滤波前，先区分回差、跳变过滤、算法平滑和测距刷新周期。
